[advice]
	statusHints = false
[checkout]
	defaultRemote = origin
[color]
	ui = false
[core]
	pager = less -F
[diff]
	algorithm = histogram
	dstPrefix = ./
	srcPrefix = ./
[merge]
	conflictStyle = diff3
	tool = diff-merge-tool
[mergetool]
	keepBackup = false
[mergetool.diff-merge-tool]
	cmd = git-diff-merge-tool "$MERGED" "$LOCAL" "$REMOTE" "$BASE"
[tag]
	sort = -committerdate
[include]
	path = .gitconfig.local
